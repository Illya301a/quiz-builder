# Quiz Builder Frontend

Next.js client for the Quiz Builder application.

## Commands

```bash
npm install
npm run dev
```

The development server is available at `http://localhost:3000`.

Additional checks:

```bash
npm run lint
npm run format:check
npm run build
```
